language: node_js
cache: yarn
script:
- yarn flow
- yarn lint
- yarn test
- yarn build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
